export type QuoteType = {
    author: string,
    content: string,
    date_created: string,
    likes: string[]
    pfp: string,
    quotee: string,
    signature: string,
}

export type User = {
    email: string,
    image: string,
    name: string,
}